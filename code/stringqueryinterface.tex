\begin{lstlisting}[language=Java, label=queryinter, caption={StringQueryInterface, grensesnittet Migrator-klassen bruker til å kjøre data\-basespørringer med.}]
package vbb.dbupgradinator;

public interface StringQueryInterface {
    public String query(String key);
    public Exception persist(String key, String aggregate);
}
\end{lstlisting}
